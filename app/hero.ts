/**
 * Defines a Hero interface with id and name properties.
 */
export interface Hero {

    id: number;
    name: string;

}

// NOTE: We need to export the Hero interface from hero.ts because we'll need to
// reference it in other files.
